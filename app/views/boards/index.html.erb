<div class="container">
  <% @boards.each do |board| %>
    <div class="card">
      <div class="card_title">
        <p><%= board.name %></p>
      </div>
      <div class="card_content">
        <p><%= board.description %></p>    
      </div>
    </div>
  <% end %>
</div>